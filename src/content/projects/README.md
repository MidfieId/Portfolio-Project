# Project content

Add one Markdown file for each project and keep its images or other media in a matching directory under `src/assets/projects/`.

Then add a short metadata object in `src/data/projects.ts`. The list and dedicated route are generated automatically.
